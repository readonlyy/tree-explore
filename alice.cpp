// Sample Alice: trivial single-node intermediate tree (K=1).
// Gives Bob no information from exploration. For testing only.

#include "grader.h"

void alice(int N, std::vector<int> P) {
    setTree(1, {});
}
